/**
 * Challenge 8: Build Word
 * 
 * Concepts:
 * - while loop: continue building until period found
 * - break: stop when period encountered
 * - string concatenation: add characters one by one
 * 
 * While Loop Pattern:
 * let i = 0;
 * let result = '';
 * while (i < array.length) {
 *   if (array[i] === '.') {
 *     break; // Stop here
 *   }
 *   result += array[i]; // Add to string
 *   i++; // Move to next
 * }
 * 
 * Note: Don't forget to increment i!
 * Forgetting i++ creates infinite loop!
 */

function buildWord(chars) {
  // Your code here
  return chars
}
