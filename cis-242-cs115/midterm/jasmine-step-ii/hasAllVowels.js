/**
 * Challenge 7: Has All Vowels
 * 
 * Concepts:
 * - for loop: check for each required vowel
 * - early return false: exit as soon as one is missing
 * - return true at end: if we get past all checks
 * 
 * Early Return Pattern:
 * Check each requirement
 * If any fails, return false immediately
 * If all pass, return true at the end
 * 
 * Loop Pattern:
 * const requiredVowels = ['A', 'E', 'I', 'O', 'U'];
 * for (let i = 0; i < requiredVowels.length; i++) {
 *   // Check if current vowel exists in letters array
 *   if (not found) {
 *     return false; // Missing! No need to check others
 *   }
 * }
 * return true; // All found!
 */

function hasAllVowels(letters) {
  // Your code here
}
