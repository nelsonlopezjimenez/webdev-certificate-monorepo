/**
 * Challenge 6: Skip and Collect
 * 
 * Concepts:
 * - for loop: iterate all elements
 * - continue: skip vowels (don't add to result)
 * - string building: collect consonants
 * 
 * Continue vs Break:
 * - continue: skip REST of this iteration, move to NEXT
 * - break: exit the entire loop
 * 
 * Loop Pattern:
 * for (let i = 0; i < array.length; i++) {
 *   if (shouldSkip) {
 *     continue; // Jump to next iteration (next i)
 *   }
 *   // This code only runs when we DON'T continue
 * }
 */

function skipAndCollect(letters) {
  // Your code here
}
